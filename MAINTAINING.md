# 维护须知

**改文案、改按钮、加截图，全在 `assets/js/content.js`**——具体改哪个字段，
那份文件的开头写着，这里不重复（两处写同一件事必然漂移）。

这份只写 `content.js` 里说不到的四件事。

## ⚠️ 三种语言都要填

某种语言缺了会**静默回落**到 `defaultLang`（现在是中文），不报错，
表现只是英文页面上突然冒出一句中文。

## ⚠️ og 标签是给爬虫看的静态兜底

微信、Slack、Twitter 抓链接预览时**不跑 JS**，所以它们看到的是 `index.html` 里
写死的那几行，不是 `content.js` 里的。换 App 名时两边都要改。

其中 **`og:image` 必须是绝对地址**。写相对路径在浏览器里一切正常，
但抓预览的爬虫不认——**表现是「分享出去没有封面图」，而你自己怎么看都是好的**。

## ⚠️ `.nojekyll` 别删

那个空文件让 GitHub Pages 跳过 Jekyll。删了不一定立刻坏，
但从此多一类「本地好好的、线上莫名其妙」的故障来源。

## 部署

GitHub → Settings → Pages → **Deploy from a branch** → `main` / `(root)`。
只设一次，之后 **push 到 main 就自动发布**，一分钟左右生效。

📌 页面用的全是相对路径，所以在 `<用户名>.github.io/<仓库名>/` 这种子路径下也能跑；
以后换自定义域名同样不用改任何链接。

## 本地预览

直接双击 `index.html` 就行。⚠️ 但**语言切换会被 localStorage 记住**——
点过一次之后自动检测就再也不跑了。要验证首次访问的效果，
开无痕窗口，或者控制台里 `localStorage.removeItem("lang")`。
